version: '2'
services:
    mongo:
      image: mongo:latest
      environment:
        - MONGO_DATA_DIR=/data/db
      volumes:
        - /data/db:/data/db
      ports:
        - "27017:27017"
    redis:
      image: redis:latest
      ports:
        - "6379:6379"